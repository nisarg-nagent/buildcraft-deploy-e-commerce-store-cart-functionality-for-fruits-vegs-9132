{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm i && cd backend && npx prisma generate && npx prisma migrate deploy && cd .. && npm run build"
  },
  "deploy": {
    "startCommand": "node backend/dist/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}